[project]
name = "template-media-player"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "homeassistant>=2024.12.5",
]

[dependency-groups]
dev = [
    "ruff>=0.14.2",
]
